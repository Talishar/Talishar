# Suppress access logging for PrivateMessagingAPI polling requests
# These are frequent health checks and clutter the logs
SetEnvif Request_URI "PrivateMessagingAPI\.php" no_log
CustomLog /var/log/apache2/access.log combined env=!no_log
